<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.2"
             xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
                 http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd">

    <!--
        Provider : EclipseLink (provider JPA natif de Payara 5).
        Le code est 100 % JPA standard, aucune API Hibernate n'est utilisée.
        Le schéma est géré par Flyway (ou le script db/install) : aucune génération
        automatique de tables côté JPA.
    -->
    <persistence-unit name="ubisenderproPU" transaction-type="JTA">
        <jta-data-source>UbiSenderProDS</jta-data-source>

        <properties>
            <property name="eclipselink.ddl-generation" value="none"/>
            <property name="eclipselink.logging.level" value="INFO"/>
            <property name="eclipselink.target-database" value="MySQL"/>
        </properties>
    </persistence-unit>
</persistence>
