target/
*.class
*.war
*.iml
.idea/
.settings/
.classpath
.project
.factorypath
src/main/webapp/ext/
*.log

# Service compagnon WhatsApp Web : dépendances, sessions et configuration
# locale (le .env contient le jeton partagé — ne jamais le versionner).
wa-web/node_modules/
wa-web/data/
wa-web/.env
# Verrou de dependances : non suivi (il depend de la plateforme d'installation).
# La reproductibilite est assuree par la version EPINGLEE dans package.json.
wa-web/package-lock.json
